# Railway Environment Variables Template
# Copy these to your Railway project settings

# ============================================================================
# Solana Configuration (REQUIRED)
# ============================================================================
# RPC URL - use devnet until smart contracts are externally audited
RPC_URL=https://api.devnet.solana.com
RPC_WS_URL=wss://api.devnet.solana.com

# Mainnet (uncomment after external security audit):
# RPC_URL=https://api.mainnet-beta.solana.com
# RPC_WS_URL=wss://api.mainnet-beta.solana.com
# Or use a paid RPC provider:
# RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# ============================================================================
# Wallet Configuration (REQUIRED - use ONE of these options)
# ============================================================================
# Option 1: Base58 private key (recommended for Railway)
# Export from Phantom: Settings > Security > Export Private Key
# ⚠️ CRITICAL: Use Railway's encrypted secrets, never commit this
WALLET_PRIVATE_KEY=your_base58_private_key_here

# Option 2: JSON array format (from solana-keygen)
# WALLET_PRIVATE_KEY=[1,2,3,4,...]

# Option 3: File path (for local development)
# WALLET_KEYPAIR=/path/to/wallet.json

# ============================================================================
# Program IDs (REQUIRED)
# ============================================================================
# Raydium AMM Program ID (mainnet)
RAYDIUM_AMM_PROGRAM=675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8

# ============================================================================
# Trading Strategy Configuration
# ============================================================================
# Options: conservative, ultra_early_sniper, momentum_scalper, graduation_anticipator
STRATEGY_TYPE=conservative

# Position limits
MAX_POSITION_SIZE_SOL=0.5
MAX_CONCURRENT_POSITIONS=3

# ============================================================================
# Risk Management
# ============================================================================
MIN_LIQUIDITY_SOL=5.0
VOLUME_THRESHOLD_SOL=10.0
HOLDER_COUNT_MIN=50

# Take profit at 2x (100% gain)
TAKE_PROFIT_MULTIPLIER=2.0

# Stop loss at 50% loss
STOP_LOSS_PERCENTAGE=0.5

# Max slippage in basis points (500 = 5%)
MAX_SLIPPAGE_BPS=500

# Position timeout in seconds (1 hour)
POSITION_TIMEOUT_SECONDS=3600

# ============================================================================
# Scanning Configuration
# ============================================================================
SCAN_INTERVAL_MS=1000
PUMP_FUN_API_URL=https://frontend-api.pump.fun

# ============================================================================
# Logging
# ============================================================================
RUST_LOG=info

# ============================================================================
# Security Notes
# ============================================================================
# 1. Use Railway's built-in secret management
# 2. Never commit private keys or keypair files to git
# 3. Rotate keys regularly
# 4. Consider using a dedicated bot wallet with limited funds
# 5. Monitor wallet balance and set up alerts
#
# ⚠️ MAINNET CHECKLIST (before switching from devnet):
# - [ ] Smart contracts externally audited
# - [ ] Audit findings addressed
# - [ ] Devnet testing completed with no issues
# - [ ] Bot wallet funded with limited SOL for testing
# - [ ] Monitoring and alerts configured

# ============================================================================
# For Railway Deployment:
# ============================================================================
# 1. Export your private key from Phantom (base58 format)
# 2. In Railway dashboard: Variables > New Variable
# 3. Set WALLET_PRIVATE_KEY = your_base58_key (Railway encrypts it automatically)
